[deploy]
startCommand = "sh -c 'uvicorn app.main:app --host 0.0.0.0 --port $PORT --workers 1'"

[environments.production.variables]
OMP_NUM_THREADS = "1"
TOKENIZERS_PARALLELISM = "false"
PYTHONOPTIMIZE = "1"
PYTHONDONTWRITEBYTECODE = "1"
DISABLE_FAISS_REBUILD = "true"
