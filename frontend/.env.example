# Copy this to .env for local development
VITE_API_URL=http://localhost:8000

# Organisation slug — get this value from running:
#   cd backend && python scripts/seed_dev.py
# It prints: ORG_SLUG=dev-org-xxxxxxxx
VITE_ORG_SLUG=dev-org-xxxxxxxx
